import React from "react";
import { SectionHeader } from "../../components/Headers";

const Origins = () => {
  return (
    <div className="origins">
      <SectionHeader headerText={"In The Beginning"} centered={false} large={false} />
    </div>
  );
};

export default Origins;